// src/App.js (Corrected structure)

// Import the component using the exact filename: dashbrdlayout.js
// We still name the imported component 'DashboardLayout' for clean JSX usage
import DashboardLayout from './components/dashbrdlayout'; 
import './App.css'; 

function App() {
  return (
    // This is the root component that renders the entire layout
    <DashboardLayout /> 
  );
}

export default App;